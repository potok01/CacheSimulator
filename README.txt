Adrian Potok
adrian.m.potok@wmich.edu
3/18/2023
CS4541

I used snake case
I used the same options and argument formatting as the reference program
I used VSCode
I fully implemented the verbose option and made sure that it matched the reference program
I used Python 3.11.2

Here is the help page for my program:

usage: cache_simulator.py [-h] [-v] -s SETS -E ASSOCIATIVITY -b BLOCKS -t TRACE

Cache Simulator using FIFO replacement strategy

options:
  -h, --help            show this help message and exit
  -v, --verbose         Enable verbose output
  -s SETS, --sets SETS  The number of set index bits
  -E ASSOCIATIVITY, --associativity ASSOCIATIVITY
                        The number of lines per set
  -b BLOCKS, --blocks BLOCKS
                        The number of block bits
  -t TRACE, --trace TRACE
                        The trace file to replay
